spring:
  datasource:
    url: jdbc:mysql://localhost:3307/madcow_user_data
    username: root
    password: Merid@12
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql